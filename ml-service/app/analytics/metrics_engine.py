"""Compute and expose metrics for optimization and monitoring."""


def summarize_metrics(results):
    """Return a short metrics summary (placeholder)."""
    return {"routes": len(results.get("routes", []))}
